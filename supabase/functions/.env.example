# Set this through `supabase secrets set`, not in the browser build.
YOUTUBE_API_KEY=
# Optional direct model generation. Sift's default Strategy AI workflow is a
# manual ChatGPT handoff and needs no model secret or additional API billing.
# Keep any future provider setting server-side; never prefix it with NEXT_PUBLIC_.
# OPENAI_API_KEY=
# OPENAI_STRATEGY_MODEL=
# Required only if direct model generation is deliberately reintroduced.
# STRATEGY_AI_MONTHLY_REQUEST_LIMIT=20
# STRATEGY_AI_MONTHLY_TOKEN_LIMIT=150000
