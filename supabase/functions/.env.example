# Set this through `supabase secrets set`, not in the browser build.
YOUTUBE_API_KEY=
# Phase 6 Strategy AI. Keep this server-side; never prefix it with NEXT_PUBLIC_.
# OPENAI_API_KEY=
# OPENAI_STRATEGY_MODEL=
# Required app-level guardrails. Generation stays disabled unless both are valid.
# STRATEGY_AI_MONTHLY_REQUEST_LIMIT=20
# STRATEGY_AI_MONTHLY_TOKEN_LIMIT=150000
